#include "main.h"
#include <stdio.h>
/**
 * int add - adds two integer and returns the result
 * Return: Always 0
 *
 */
int add(int, int)
{
	int n;

	n = add(89, 9);
	printf("%d\n", n);
	return (0);
}
