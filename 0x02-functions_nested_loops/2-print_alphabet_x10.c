#include "main.h"
/**
 * print_alphabet_x10 - prints the alphabets in lowercase x10
 */
void print_alphabet_x10(void)
{
	int p;
	int i;

	for (i = 0; i <= 10; i++)
	{
		for (p = 'a'; p <= 'z'; p++)
		{
			_putchar(j);
		}
		_putchar('\n');
	}
}
